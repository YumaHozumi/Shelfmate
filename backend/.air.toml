# .air.toml
[build]
  cmd = "go build -o ./tmp/main ./main.go"
  bin = "./tmp/main"
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["assets", "tmp", "vendor"]

[log]
  level = "debug"

[color]
  main = "cyan"
  watcher = "magenta"
  build = "yellow"
  app = "green"
